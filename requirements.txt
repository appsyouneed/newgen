# Core (torch installed separately in setup.sh)
# torch>=2.13.0
# torchvision>=0.28.0

# Pinned compatible stack
huggingface-hub>=0.34.0
transformers>=5.3.0,<5.6
diffusers>=0.37.0,<0.39.0
safetensors>=0.4.0
numpy<2.1

# Model dependencies
accelerate>=1.0.0
sentencepiece
peft
torchao

# Mage-Flow (picgen model — replaces Qwen Image Edit)
# NOTE: flash-attn is a runtime dependency of mage-flow but must be installed
# SEPARATELY after torch, with --no-build-isolation. On Python 3.12+ use the
# pre-built wheel approach (setup.py auto-fetches from GitHub Releases):
#   pip install flash-attn --no-build-isolation
# If that fails, install from the pre-built wheel repo:
#   pip install https://github.com/mjun0812/flash-attention-prebuild-wheels/releases/download/v0.9.52/flash_attn-2.8.3+torch2.13cu130-cp312-cp312-linux_x86_64.whl
git+https://github.com/microsoft/Mage.git#subdirectory=mage_flow
einops>=0.8.0
pydantic>=2.0
loguru>=0.7.0

# Image/Video processing
opencv-python
imageio
imageio-ffmpeg
Pillow

# UI and utilities
gradio>=6.0
ftfy
tqdm
typing-extensions

# MMAudio
git+https://github.com/hkchengrex/MMAudio@d1d78adc63f59329127e73fa003c29cab6714107#egg=mmaudio
open_clip_torch
av
librosa
omegaconf
scipy
soundfile
PyYAML
